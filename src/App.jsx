function App() {
  console.log(import.meta.env.VITE_APPWRITE_URL);
  return (
    <div
      style={{
        minHeight: "100vh",
        width: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      fjnj
    </div>
  );
}

export default App;
